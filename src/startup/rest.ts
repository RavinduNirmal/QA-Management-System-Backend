/**
 * This file is maintained for backward compatibility.
 * All routes are now defined in routes.ts
 */
import router from './routes';

// Re-export the router with the same module pattern
export default router;